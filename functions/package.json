{
  "name": "ccat-mastery-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for CCAT Mastery – Stripe webhook handler",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "serve":  "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin":    "^12.0.0",
    "firebase-functions": "^5.0.0",
    "stripe":            "^14.0.0"
  }
}
